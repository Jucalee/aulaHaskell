<div .container>

    <h1>Listagem de questões
    
    $forall (pergunta) <- perguntas
        <div .col-md-8>
            <h3>Questão: #{perguntaDescricao pergunta}
            <ul .list-group>
                $forall (alternativa) <- alternativas
                    $if id pergunta == perguntaid alternativa
                        $if alternativaiscorreta == true
                            <li .list-item><b>#{alternativaDescricao alternativa}
                        $else
                            <li .list-item>#{alternativaDescricao alternativa}
